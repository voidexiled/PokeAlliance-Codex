# Current Status

Last updated: 2026-09-24
Repository: `C:\Users\jalom\Documents\ChatGPT\PokeAlliance-Codex`  
Branch: `master`  
Git history: initial publication commit `91040a2`; subsequent publication and CI-fix commits are on `master`

## Corte 0 redesign (current work)

The owner-approved redesign of D-014 is implemented in the uncommitted working tree, as [CORTE_0_CODEX_TOOLTIP_SPEC.md](CORTE_0_CODEX_TOOLTIP_SPEC.md) specifies and [IMPLEMENTATION_PLAN.md](IMPLEMENTATION_PLAN.md) orders it, except Comercio phase B (M14), which is in progress. Milestones M1 to M13 and M15 are integrated: M11 closed F2 (the wiki pages of §8), M12 is phase A of Comercio (§9), M13 is the account page and the Guild rewrite (§10) and M15 closed the cut; the technical decisions each one took are D-015 to D-026 in [DECISION_LOG.md](DECISION_LOG.md). The status of the cut is «implemented and technically verified; visual acceptance pending»: the owner's page-by-page review of the local preview against the boards (S22, OG-8) is the acceptance left, and no automatic test replaces it. Compare Pokémon (M16, F5) is deferred by the owner (2026-09-23), so `/{l}/herramientas/pokemon/` stays the placeholder of §8.14. Nothing of the redesign is committed or deployed.

- «Otros» classified (2026-09-25, uncommitted, owner rules): 1,151 of the 1,165 items of `content/items/otros.json` moved to their category (670 Creature Items, 228 Stones including the Boost Stones and the Mega Stones, 72 Orbs, 104 Furnitures, 12 Foods, 6 Addons, 3 Helds, 5 Consumable, 3 Utilities, 3 General Items) and to four new site categories after Furnitures: `boosters` (9), `diamond-utilities` (6), `packages` (28) and `toys` (2, the Kanto and Johto Toy Boxes). 14 Easter items stay in «Otros». `held.ranura` accepts `null` for the random «Held Item (Tier: n)». The export has no Toy Box contents, so no toy record was created. `pnpm content:datamine` dry-run leaves every move in place. See docs/REGISTROS.md.
- Item categories, second round (2026-09-25, uncommitted, owner decisions): two new site categories, `mega-stones` (the 35 Mega Stones with `mega` and Clefablite, out of Stones) and `shiny-creature-items` (417 Creature Items that only shiny Pokémon drop, shiny evolution items such as Master Belt included); the Kanto, Johto and a new Hoenn toy box record moved to Packages; 1,080 toy records created in Toys from the client's inspection names (sprites extracted, `mercado` from the Market listings), each Gen 1/2/3 species toy with `obtencion.cajas` naming its region's box (153 Kanto, 125 Johto, 139 Hoenn; 663 without box: Gen 4+, shiny, Mega, regional-form and event toys), chance `null`; 18 items whose game text grants a timed or % bonus moved to Boosters (12 Foods, 5 Easter items of «Otros», Guardian Elixir). `categorias.json` marks the 13 Market categories with `mercado: true`; the item page's Market block shows only with `mercado: true` and names the category only when it is a Market one; the Items nav is «Categorías». To stay within §13.6 the search index is packed (`src/lib/search/index-file.ts`: an item of a category page is a row; the palette loads the reader and the ranking in one deferred chunk) and `/{l}/items/datos.json` rows drop trailing nulls. See docs/REGISTROS.md.
- Animated items (2026-09-25, uncommitted, owner rule «what the client animates, the site animates everywhere»): `extract-game-sprites.mjs` now writes every item with more than one DAT phase as the strip of its phases (`modo: animacion`, 110 ms per phase: the DAT has no durations) — 388 of the 1,516 client item sprites — and re-renders the registry copies of animated items (`ITEM_COPIES`: `ui/diamond` = item 3028 with 22 phases, `ui/categorias/helds` = 30858 with 14, and the already animated `ui/indice/items`, `ui/indice/actividades`, `ui/sistemas/boost`). `spriteData` now plays an `animacion` entry unless `animado: false` or a frame is asked for, so the menu, the Items category tabs, slots, tooltips, lists, pickers, item pages, Destacados and Comercio all animate (reduced motion keeps frame 0); the lists carry an animated client sprite as `"<clientId>x<frames>"`; equal-duration animations use one `steps(n, jump-none)` keyframe block named `ac-sprite-<n>x<ms>`. Spec lines that kept the Diamond still outside Comercio (§6.3, §7.5.3, V6-3) are superseded by this rule.
- Client update round (2026-09-25, uncommitted): the sprite extraction reran against the updated client (`C:/Users/jalom/Documents/pka_output_decrypt/2026-09-25_02-55-44`, decrypted after the 2026-09-23 update): the 33 Gen 4 items (client ids 50743–50801) got their sprites and `apilable`, and 26 Pokémon outfits the update redrew changed (Nosepass, Probopass, Timburr line, Staravia, Vespiquen, Mismagius, Yanmega and shinies; 9 now animate). Pokédex portraits are unchanged. `ui/diamond` (the Diamond item and the currency) is now DAT item 3028's frame 0, the blue gem the game shows. The client has no catch rates, so a Ball's «Tasa de captura» row appears only once `ball.tasa` is written (no «—» row). A tier without `maxBrokes` has no tooltip and no trigger (Pokémon hero chip, Cards/Lista value, Tier list label, Tier filter). Pokémon panels show `efectividad` as element icons (Débil a ×2/×1,5, Resiste ×0,5/×0,4, Inmune), in list islands from `/{l}/paneles.json` (`tipos` + a letter code per Pokémon, src/lib/game/pokemon-panel.ts) and in server-rendered panels from the record; Comercio listing panels add a Mega form's Mega Stone. Comercio's own Pokémon panels (`TradeListRoot` `pokemonPanel`) do not load `paneles.json`, so they have no effectiveness rows yet. The Tier list's initial JS is 109,6 of 110 KB.
- Tooltip round (2026-09-25, uncommitted, D-031): item panels list every shop offer, Battle Pass level, calendar day, recipe and task; every Poké Ball shows «Tasa de captura» once the owner writes `ball.tasa` (neither the client nor the export carries it); Diamonds show «Se usan en: Diamond Shop»; Comercio listing panels add Nº, Rasgos, Habilidades and each held item's effect; nine system tooltips are filled. `ui/balls/*` are the client's ball sprites. The sample Comercio registry moved to `tests/fixtures/comercio/` (test-only). The sidebar «Inicio» sprite lines up with the group sprites.
- Tooltip completeness fixes (2026-09-25, uncommitted): the item page's facts panel shows a held item's Ranura and Tier and a Ball's Tasa de captura (once known), Más efectiva con and Aura; the stage of every item card (search Cards, Pokémon drops, system items) and of every Comercio listing card opens the item's or Pokémon's full panel; the listing detail's hero art opens it too, and an item listing's hero adds the game text and every panel row (shops, Battle Pass, prices, Mercado, Ball and held rows); listing panels name a Ditto's memories («Ditto Memory»); the home's 18 element links open their element panel; Mega forms name their Mega Stone (`megaStoneByForm`, src/lib/game/item-facts.ts). The client's «Sin descripción.» Pokédex placeholder is now `descripcion: null` (109 records; the importer maps it to null). Still owner data: Ball catch rates (`ball.tasa`, not in the client or export), Pokédólares `moneda` lists (no panel without them), Mega Charizard Y's level/tier/role, and the `borrador` aura icons and `ui/comercio/item`.
- Account pages round (2026-09-25, uncommitted, D-030): «Mi perfil», «Mis anuncios» and «Mis operaciones» of the header menu open `/{l}/cuenta/perfil/`, `/{l}/cuenta/anuncios/` and `/{l}/cuenta/operaciones/`, drawn in the same frame as `/{l}/cuenta/` (`AccountFrame.tsx`; island `AccountPage.tsx`, bodies in `src/components/account/pages/`). `ProfileHub.tsx`, `profile-hub.css` and `src/routes/comercio/operaciones.astro` are gone; `/{l}/comercio/operaciones/` is a 302. Checked on the local dev server (Podman Supabase, launch entry `cuenta-p6`) at 1440 and 375.
- Dirección C round (2026-09-24, uncommitted): the owner chose Direction C of the Claude Design canvas for the filters. The brand is «PokeAlliance Wiki» (visible strings, dictionaries, `scripts/seo/check-dist.mjs`). Pokédex and Tier list use the `FilterToolbar` of `src/components/filters/` (Tipo AND max 2, Tipo de moveset OR, Tier ladder, Variante, Generación, active tokens, per-view «Por página»); the Comercio pickers use `PickerFilterBar.tsx` in the same style. Shiny is a golden glow on the art (`shiny-glow`, `glow-shiny` tokens), missing art is the client Pokédex «?» (`ui/pokemon-desconocido`), the Slots view is one 12-column inventory, Cards and Lista show Tipo/Moveset as `ElementIcon`s and Tier as `TierValue` with the «Max brokes: —» tooltip. `content/tiers.json` (new registry, see REGISTROS.md) hides ULTIMATE: its Pokémon show «—» and it is out of the Tier filter and the Tier list. The Tier list has Slots (rows tinted with the `tier-row-<id>` tokens of the board) and Lista only. Pending: the owner's visual review in a browser; the Pokémon tooltip rows of the board (Nº, Max brokes, icon values) are not built.
- Game sprites (2026-09-24, uncommitted): `scripts/assets/extract-game-sprites.mjs` gave 1,498 items their client inventory sprite (`items/cliente/<clientId>`; 33 clientIds are newer than the decrypted DAT), 1,023 of 1,035 Pokémon an outfit (south idle frame, animated strips for 124 `animateAlways` outfits) and 56 Pokémon their client portrait. The list files and props carry a client item sprite as its `clientId` (`listItemSprite`/`expandListSprite`), the filter icons read a build cut of the registry (`virtual:ac-picker-sprites`) and the `content:check` tests link the generated sprite folders. The `/{l}/pokedex/` initial JS was already 110.2 KB at `83ac8b2` (over the 110 KB of §13.6) and is 110.3 KB now. Usage and limits: `scripts/assets/README.md`, `docs/LOCAL_CLIENT_RESEARCH.md`.
- M10 (2026-09-22): the Inicio (`/{l}/`, template A: `HomeIntro`, the search trigger, «Destacados» and the bento of index panels and the Worlds table) and the search results page (`/{l}/buscar/`, template B with the `buscar` list island) moved to `PageLayout`. The pinned «Destacados» group of the menu and the Destacados of the pages come from one list (`buildFeatured` of `src/lib/nav/groups.ts`), so they always show the same entries. `content/destacados.json` (four `borrador` placeholders) and `content/mundos.json` are new owner registries, documented in [REGISTROS.md](REGISTROS.md). The old home search, the old results page and `searchRecords` are gone; `ServerSaveStatus.tsx` stays in the tree with no consumer until M11 answers Q7. After the two M10 reviews: the unit tests no longer depend on what the owner has in `destacados.json` and `mundos.json` (publishing an entry, changing a world or deleting either file keeps `pnpm test` green); the Buscar list has its own unit and e2e tests; a `q` longer than the 100 characters of the field is cut in the URL; HomeIntro and FeaturedCard never draw a pixel sprite above 3x (S7); and the 1 px snap of the menu sprites moved from `style` to `sidebar.css` (§3.7). D-022 records these decisions.
- M11 (2026-09-23): the last routes of §8 moved to `PageLayout` — Cambios (`/{l}/cambios/`, template C over `ChangeTimeline`), the activities index and one page per activity (`/{l}/actividades/`, `/{l}/actividades/{id}/`), the tools index (`/{l}/herramientas/`) and the two `noindex` placeholders of template E, the map (`/{l}/mapa/`) and Comparar Pokémon until F5 (`/{l}/herramientas/pokemon/`) — and the 404 is the on-demand `src/pages/404.astro` in the language of the requested path. `/` is a 302 to `/es/` from configuration, and `/{l}/mapa/aportar` and `/{l}/guias` answer 302 to the map and the activities, with and without the trailing slash. Only `/{l}/comercio/` (M12) and `/{l}/herramientas/guild/` (M13) still render with `AppLayout`. `content/cambios.json` is a new owner registry (empty: the page says «Aún no hay cambios publicados.»), and the texts of `content/quests.json` are now per language, the one published activity still in English only; both are documented in [REGISTROS.md](REGISTROS.md). The menu gains its «Actividades» group, the Inicio its «Actividades» panel and the search index its `actividad` group. `PokemonExplorer.tsx`, `ServerSaveStatus.tsx` (Q7 is still open) and the pages of the retired routes are deleted. After the two M11 reviews, `check-dist.mjs` and `perf:budget` also measure the on-demand 404 at `/es/no-existe/` and `/en/no-existe/`, rendered through the server function of `.vercel/output` (`scripts/lib/bajo-demanda.mjs`), so its link to the shared stylesheet (S17) and its §13.6 budgets are gates of `pnpm ci`, and NF1 checks its `<title>`. D-023 records the decisions.
- M12 (2026-09-23): Comercio phase A (§9) moved to `PageLayout` over static sample data, with no Supabase, no account and no contact. `/{l}/comercio/` is the list of template B (search, type tabs, the Mundo / Moneda / Precio / Valoración filters, three orders, Cards grouped by type, Slots and Lista, and the listing tooltip); `/{l}/comercio/anuncio/{id}/` is the detail (template F, with the fixed sheet), `/{l}/comercio/vendedor/{handle}/` the seller profile (template H, with the listings and the reviews) and `/{l}/comercio/publicar/` «Crear anuncio» (template G: the form, its live preview, «Copiar anuncio» and the draft kept in the browser). The sample registry `content/comercio/` (16 listings and 5 «Ejemplo» sellers, all `borrador`) is read only with `COMERCIO_DEMO=1` (the development server of the tests, demo and visual builds): a production build shows only «Aún no hay anuncios.» and «Crear anuncio», has no detail, profile or data route, and no file of its output carries a listing id or a seller name (CA-9.1, checked by `node scripts/test/build-comercio-fases.mjs` and `pnpm seo:check`). `src/integrations/comercio-fases.ts` sets the render mode of each Comercio route by phase (`COMERCIO_PUBLICO`, §9.3) and fails the build when a Comercio page exports `prerender`. `getMonedaDiamantes()` reads the `moneda` object of `content/items/diamantes.json` (null lists today, so the Diamonds amounts stay text), the listing tooltip lists every trained skill, and the old session compositor `TradeDesk.tsx` is deleted. [REGISTROS.md](REGISTROS.md) documents the `moneda` object and the sample folder. D-024 records the decisions.
- M13 (2026-09-23): Guild (`/{l}/herramientas/guild/`) moved to `PageLayout` as one island, `GuildRoot`, over the unchanged calculation engine and the new `src/lib/tools/guild-analytics.ts` (Resumen with its period filter, Por día, Por semana, Miembros with the member dialog, inactivity, CSV and PNG exports); its prerendered HTML is only the crumbs, the h1 and a busy region (CA-10.13). Local mode keeps the browser workspace; account mode reads and writes the guild through Supabase with owner, officer and member roles. `/{l}/cuenta/` (only in a build with the public Supabase settings, `noindex`) has «Acceso» and «Guilds»: create a guild in a world of `content/mundos.json`, one-use 7-day invitation links, the accounts with «Quitar», and «Eliminar guild». The migration `supabase/migrations/20260923150000_guild_admin.sql` is applied to the LOCAL stack only, and its permission cases run against it (`tests/supabase/guild-rls.test.ts`); supabase-js is loaded on demand, outside the initial JS of every page. The old Guild tool, its persistence panel and the shadcn kit (`src/components/ui/`, `src/lib/utils.ts`, `components.json`) are deleted. D-025 records the decisions.
- M15 (2026-09-23): the closure of §3.10 step 4, run ahead of M14 because no route imports `AppLayout` since M13. `src/layouts/AppLayout.astro`, `src/components/wiki/AllianceWordmark.astro`, `src/styles/legacy/` (the five old sheets), `src/components/sprites/` and `localeCopy` and `localeLabels` of `src/i18n/config.ts` are deleted, and with them every old variable and class (S18); the language names of the language menu moved to the dictionaries as `shell.languageName`. `src/styles/global.css` is only the entry of §3.6. `package.json` drops `tw-animate-css`, `@base-ui/react`, `class-variance-authority`, `cn` and `shadcn`; `lucide-react` stays for the `Glyph` module. `scripts/lib/rutas-migradas.mjs` returns «todas» (`plantillasMigradas` is `'*'`): `pnpm design:check` covers all of `src/` and `check-dist.mjs`, `perf:budget` and `seo:check` all of the output, with no migration exclusions (ESLint and `i18n:check` lost theirs too), so the pages M14 adds on `PageLayout` enter every gate directly. The route inventory of §8.0.1 stays in the same module as `plantillasDelSitio`, which answers WG5 of the menu and BU6 of the search index (`esRutaDelSitio`) and RZ3; the parity routes are `plantillasParidad` (D-026). The gates read a build without the public Supabase settings, as CI does: with them the build writes `/{l}/cuenta/`, which §13.6 has no budget row for. `src/components/map/MapExplorer.tsx`, `content/map/*`, `src/lib/map/*` and `tests/map/client-map.test.ts` stay unused for the map's own cut (R9, §15). The CI workflow keeps the three jobs of §14.6 with no migration step left. `DESIGN.md`, `.impeccable/design.json`, `docs/DESIGN_DIRECTION.md` and `docs/CONTENT_PLAN.md` had already been rewritten in M13.
- Waiting on the owner: the page-by-page visual acceptance of the local preview (S22); the real entries of `content/destacados.json`, `content/mundos.json` and `content/cambios.json`; the sprites `ui/inicio`, `ui/indice/*`, `ui/cambios`, `ui/herramientas/guild`, `ui/herramientas/mapa`, the activity sprites and 16 px sprites for the menu's «Destacados»; the Spanish texts of the activities; the approval of the seven new 390 baselines of M11 (`cambios`, `actividades`, the activity page, `herramientas`, `mapa`, `comparar` and the 404), of the four of M12 (`comercio-390`, `comercio-anuncio-390`, `comercio-vendedor-390` and `comercio-publicar-390`), of `buscar-390` and of the earlier ones D-021 lists; the lists of the `moneda` object of `content/items/diamantes.json`; the remote application of `20260918220000_remove_provenance.sql` and `20260923150000_guild_admin.sql` (OG-1), after which the legacy retry of `src/lib/supabase/guilds.ts` goes; the `guild` visual case and its goldens; the open questions Q6, Q7, Q8, Q9, Q13, Q14 and Q15; and, for Comercio phase B (M14), the decisions D-B1 to D-B7 of §9.13.
- No milestone of the plan owns yet: the game tooltip of the palette's active option (§7.9.5) with the `tip` of the search index (§8.6), the «Inicio» section of the Tarjetas board in `/_paridad/tarjetas/` (D-022), and a `DataTable` with `scroll` that is the containing block of what it places absolutely, which M12 does only for the Comercio Lista (D-024).
- In progress: M14 (Comercio phase B: local Supabase, verification, trades, reviews and moderation), written and tested only against the local stack and behind `COMERCIO_PUBLICO`; its public launch is the gate of §9.2. Deferred by the owner: M16 (Compare Pokémon).

## Active phase

**Corte 0 (D-014) closed except Comercio phase B (M14, in progress); owner visual acceptance (S22) pending.**

Phase 0 planning, Phase 1 research, Phase 2 data-model consolidation, Phase 3 technical foundation and the first Phase 4 Wiki Core content slice are complete with documented limitations. Corte 0 reopened the Phase 3A design gate and rebuilt every route on `PageLayout`: the visual contract is the D-014 design (the approved canvas, the Design System, whose tokens the repository carries in `src/design/tokens.json`, and [CARD_GRID_SYSTEM.md](design/CARD_GRID_SYSTEM.md)), taken into code by the Corte 0 specification. Tools: Guild runs in the new shell (M13); the map and Compare Pokémon are placeholder pages until their own cuts. Accounts: `/{l}/cuenta/` (M13) with the guild administration, tested against the local Supabase stack. Marketplace: Comercio phase A (M12) over sample data; phase B (M14) is in progress, local only. The remote Supabase project `PokeAlliance Codex` has the first seven migrations applied; the later ones are applied only to the local stack until the owner authorizes their remote application (OG-1).

The game-knowledge expansion is now in active implementation. The public Pokédex serves the 910 variants of the current roster from `content/pokemon.json`; the species totals of 530 headline entries, 526 normal rows and 507 distinct Pokédex numbers remain unreconciled. **Provenance was removed everywhere on 2026-09-18 (D-012):** site data now lives in owner-editable JSON under `content/` with Spanish keys and `null` for unknown values; the source/evidence registries, staging and normalized datasets, coverage reports, provenance schemas, PowerShell validators and research importers were deleted; the UI no longer shows source disclaimers. The eighth local migration (`20260915192641`) models drop rates, spawn relations, progression, activities, economy and change events without provenance columns, and the new local migration `20260918220000_remove_provenance.sql` drops the provenance tables, columns and enums. Neither has been applied to the linked remote project.

The interface before Corte 0 (the RubinOT-reference shell of `AppLayout`, the image-led Pokédex grid, the Guías mission timeline, the first Compare, map and Guild tools and the shadcn kit) is no longer on the site; the «Completed» entries before 2026-09-19 describe it as history. Official remote Pokémon images are still linked, not copied.

The original-client outfit preview reads the outfit of each Pokémon from `content/outfits.json` and its frames from the sprite registry `public/sprites/sprites.json` (D-011). Bulbasaur `#2`, Charmander `#5`, Charizard `#7`, Shiny Charizard `#509` (reconciled against the owner's ObjectBuilder screenshot) and Squirtle `#8` are available in the Pokédex. All four directions use only the DAT phase-0 idle frame; walking phases are retained as metadata but never shown. The existing portraits are unchanged. The auras come from `content/auras.json`: Alliance (`outfit_alliance`) and Premier (`outfit_rainbow`), both verified in-game by the owner, render as continuously animated WebGL outlines adapted from the owner's already-decrypted shaders. The aura pass now uses the actual padded texture texel for its one-pixel contour and a relative clock, matching the supplied 30 fps gameplay clips more closely; this corrects the previous 8-pixel padding/64-pixel sampling mismatch and makes the visible gradient travel top-to-bottom after compensating for WebGL UV orientation. The 20 idle frames now live in `public/sprites/outfits/<outfitId>/`, byte-identical to the owner's dump, and are reproducible through `scripts/assets/extract-outfit-preview.mjs` or `pnpm assets:outfits -- --mapped --registrar`. This remains a local preview: asset redistribution rights remain open; no commit or deployment has been made. The focused outfit Playwright flow now checks idle URLs, size-aware canvas bounds, and that both aura modes continue animating while the sprite remains idle. `pnpm check` and `pnpm lint` pass; owner acceptance is pending.

The owner has now asked to begin community Trade/RMT and added mandatory verified email and phone accounts plus transaction-linked 0–5 buyer reviews with screenshot evidence. Official PokeAlliance terms checked 2026-09-16 allowed RMT outside official channels but prohibit advertisements in their in-game chat, Discord and forums; **terms version `v1789689677` (2026-09-17) removed the explicit RMT permission** (verified live 2026-09-18). The owner decided to keep a real-money lane via verified seller contact channels and no payment gateway (D-009) and approved the Codex Tooltip design system with Corte 0 first (D-010). `docs/TRADE_PRODUCT_PLAN.md` records the model, design, source, account/review gates and unknown catalogs. Since M12 (D-024), `/es/comercio/` and `/en/comercio/` are the phase A market of §9 over the sample registry of `content/comercio/`, which only builds and servers with `COMERCIO_DEMO=1` read: a production build shows the empty list and «Crear anuncio», whose form copies the text of a listing and publishes, stores and charges nothing. The session compositor `TradeDesk` is deleted. Every image comes from the sprite registry (D-011); the installed client's corresponding nominal `.png` files start with `PKA1` and cannot be used as ordinary web images, and no protection was bypassed or client asset copied. Source/usage rights for a distributed sprite package remain unresolved. Existing Supabase local config has email confirmation disabled and SMS disabled, so verified-account publishing and reviews are **not implemented** or claimed functional; they are the scope of M14 (in progress), local only. No marketplace migration has been applied.

## Completed

Entries dated before 2026-09-18 mention provenance files, validators and reports that D-012 removed, and entries before 2026-09-19 describe the interface that Corte 0 replaced; both are kept as history.

- Corte 0 redesign (D-014, 2026-09-19 to 2026-09-23): milestones M1 to M13 and M15 of [IMPLEMENTATION_PLAN.md](IMPLEMENTATION_PLAN.md), described in «Corte 0 redesign» above and in D-015 to D-026. Comercio phase B (M14) is in progress, Compare Pokémon (M16) is deferred by the owner and the owner's visual acceptance (S22) is pending.

- D-011/D-012 review fixes (2026-09-18): `pnpm content:check` now validates every JSON file in `content/` (Pokémon, moves, quests, locations, rotations, system items, map markers and floors) against new JSON Schemas with `"$schema"` and no unknown fields, checks unique ids, move and outfit Pokémon references and map floor image sizes, reports a broken file instead of crashing, and keeps checking the rest after a schema error in one record. `categorias.schema.json` fixes the 14 Market categories and their order; the Zod mirrors read categories, tiers, variants and elements from the JSON Schemas. The build parses every content file with Zod (`src/lib/content/content-schema.ts`); map data moved to the server-only `src/lib/map/map-data.ts`, and islands stay free of Zod. Sprite animation names can no longer collide (`/` becomes `--`). Comercio shows every game image through the sprite registry; the Diamond keeps its entrance animation. The guild panel shows the snapshot date instead of the file name and no longer sends it; `20260918220000_remove_provenance.sql` also drops `source_locator`. `MASTER_PLAN.md`, `RESEARCH_PLAN.md` and `ARCHITECTURE_PROPOSAL.md` no longer prescribe provenance.

- D-011 owner-editable registries (2026-09-18): `content/items/categorias.json` (14 Market categories) and 13 per-category item files with placeholder records (real names for Diamond, Alliance/Premier/Ultra Ball and the five stones; `clientId` and NPC prices `null`; all `borrador`), `content/outfits.json` (replaces `data/pokemon-outfits.json`; one placeholder addon, outfit `1005`), `content/auras.json` (Alliance, Premier), JSON Schemas in `content/schemas/` with a Zod mirror in `src/lib/content/registry-schema.ts` and loaders in `registry.ts`; the owner's example sheets moved from `public/images/` to `public/sprites/` with `sprites.json`; pure resolver `src/lib/sprites/resolve.ts` and `Sprite.astro`/`Sprite.tsx` (object-position frames, per-frame `@keyframes`, reduced motion shows frame 0); real Diamond sheet in Comercio; `pnpm content:check` (in `pnpm ci`); `pnpm assets:outfits -- --registrar`; owner guide `docs/REGISTROS.md`. `public/item-3028-sprites.png` named in the request was not present in the repository.

- D-012 provenance removal (2026-09-18): moved site data to `content/` (`pokemon.json`, `moves.json`, `system-items.json`, `quests.json`, `locations.json`, `rotations.json`, `map/markers.json`, `map/floors.json`); rewrote `src/lib/content/repository.ts`, `types.ts`, `src/lib/tools/pokemon-roster.ts` and `src/lib/map/client-map.ts` around typed records; removed provenance types, fact wrappers, the Pokédex "record data" block, the map source note, the map review-state table and the Trade "not verified" notes; replaced the PowerShell roster importer with `scripts/content/import-roster.mjs` (`pnpm content:roster`); removed input hashes and documented-hash checks from the outfit tools' outputs and from `public/images/outfits/manifest.json`; dropped `validate:data` and `validate:phase2` from `pnpm ci`; wrote migration `20260918220000_remove_provenance.sql` (local only).
- Preserved all three original prompts verbatim under `docs/requirements/`.
- Added the authoritative specification index and precedence rules.
- Created Phase 0 planning documents and initial `AGENTS.md`.
- Created initial knowledge/research directory structure.
- Registered ten source surfaces and nineteen evidence records in machine-readable form, including the owner-provided Discord transcription, local-client observation, public map-source profile, official quest sample and the OTMM format reference.
- Inventoried the official-candidate wiki route surface and representative pages.
- Inspected the community wiki repository at commit `0135ccdef08109ff04dfc3343dbeb8fdfe9989b1` (2026-06-22).

- Inventoried community datasets and classified them as staging candidates/lead data due to missing per-record provenance.
- Classified `tanjirokamadoserver/PokeMonster` as historical hypothesis material only.
- Created evidence, conflicts, unknowns, glossary, terminology sample and initial coverage report.
- Validated that all repository JSON parses, source IDs are unique and evidence source references resolve.
- Preserved a user-provided transcription of the current Discord rules as high-volatility, not-yet-directly-verified evidence.
- Set Discord changelog ingestion to manual-only for now because bot access to the official Discord is unavailable; authorized automation is documented as optional future work and does not block Phase 1.
- Defined Server Save as a recurring civil-time rule at `00:00[America/Sao_Paulo]`, converted to visitor-local time through a centralized Temporal adapter.
- Added supplied Discord rule 6: client/wiki information may be used to build community wikis and tools, and community knowledge contributions are encouraged. Wholesale redistribution rights for individual copyrighted assets remain a separate scope question.
- Recorded explicit owner authorization for read-only local-client research and found the installation at `C:\Users\jalom\AppData\Local\PokeAlliance Games\PokeAlliance`.
- Completed a metadata-only first inventory: 8,075 files (~810 MB), including Lua/OTUI modules for Pokédex, moves, item drops, quests, hunts, NPCs, minimap, market, Stars and other game systems, plus a 672,128-byte `minimap.png`.
- Recorded owner authorization for targeted read-only research of local PokeAlliance client files, including map formats and data relevant to a future interactive map, under `docs/LOCAL_CLIENT_RESEARCH.md`.
- Added a read-only OTMM parser and a stable, hashed profile of the historical `minimap854.otmm` snapshot: OTMM v1, 33,501 blocks and floors 0–15. A current local roaming refresh was separately parsed at 33,538 records and is used only for the derived Phase 5 preview.
- Established the protected-client boundary: sampled packaged Lua/OTML/PNG-labeled assets were not bypassed or decoded.
- Added draft required-field matrices for Pokémon variants, moves, locations/hunts, items, quests, tier/rotation assertions and map features.
- Added shared staging/normalized research schemas, a wiki-to-staging demonstration and source-specific importer contracts.
- Added an automated research validator and generated the first measured normalized-data coverage baseline.
- Added supported normalized samples for one move (`Scratch`), one location (`Saffron`) and one item (`Normal charger`) from current administrator-wiki evidence.
- Formalized the two primary Phase 1 acquisition families: publicly delivered wiki pages/application resources and owner-authorized local or supplied PokeAlliance client files.
- Added a Git-ignored `research-inbox/client-files/` for owner-supplied client research inputs.
- Inventoried the supplied inbox: 8,882 files (~926 MB), including the full client, launcher caches and runtime state; sensitive WebView/account/profile branches were explicitly excluded, with the specifically authorized guild export inspected structurally.
- Discovered a structured 20-record launcher news feed suitable for local changelog ingestion and a versioned launcher Terms of Service cache.
- Updated changelog architecture: local launcher-feed ingestion is available now; Discord remains manual-only.
- Implemented and verified the launcher-feed profiler; its current source hash reproduces 20 news records and 2 banners without persisting raw HTML.
- Inventoried minimap module and marker assets: waypoints, coordinate search, map flags, floor/zoom controls and nine named marker categories. Their placement data remains unresolved because module/image bytes are protected.
- Extracted 119 actual `Minimap.flags` records from runtime `config.otml`, including descriptions, icon IDs and `(x, y, z)` coordinates across floors 1–9. Added a conservative normalized map-feature sample.
- Formalized the interactive-map layers and community-contribution workflow. The client has separate Pokédex, hunt-finder and item-locator affordances, but the Pokémon coordinate payload source is still unverified.
- Re-profiled the public map-related sources: the official roster API has no coordinate fields, sampled official detail payloads have no structured spawn table, and pinned community location/hunt/task datasets have no x/y/z records. Added a reproducible profile and registered `UK-010`.
- Added the manual map-contribution capture protocol for Pokémon, NPC, quest and travel-point observations, with explicit pending/reviewed states and Temporal-preserved capture instants.
- Added and measured an official quest sample for `Porygon Quest: Dr. Vektor`, preserving its requirements, steps, rewards and missing coordinate/repeatability fields.
- Added and measured a tier/rotation assertion sample for special Shiny availability, keeping exact appearance rates explicitly unknown.
- Published the complete Phase 1 exit audit: [PHASE_1_EXIT_REPORT.md](PHASE_1_EXIT_REPORT.md). It records all met criteria, measured coverage, safe-to-model areas, weak areas and the Phase 2 handoff.
- Registered the Rule 6 scope/numbering difference between the supplied Discord rules and launcher Terms of Service as conflict `C-003`.
- With explicit owner authorization, inspected the structure of the guild export and related player/ranking caches. The guild export contains 25 members with contribution, dailies, level, rank, status and last-login fields, but no stable member ID.
- Added the future guild snapshot contract and privacy-preserving profile: [GUILD_SYSTEM_RESEARCH.md](GUILD_SYSTEM_RESEARCH.md), `data/schemas/guild-snapshot.schema.json` and `data/research/guild-export-profile.json`.
- Consolidated the definitive data model in [DATA_MODEL.md](DATA_MODEL.md), including identity/variant rules, claims/provenance, localization, Temporal semantics, map contributions, guild snapshots, import idempotency and correction workflow.
- Replaced the provisional architecture with [ARCHITECTURE.md](ARCHITECTURE.md), keeping `ARCHITECTURE_PROPOSAL.md` as historical context.
- Added the Phase 2 canonical interchange schema at `data/schemas/canonical-record.schema.json` so reviewed records can project into typed entities and field-level claims without confusing staging statuses with publication statuses.
- Designed the Supabase migration set in `supabase/migrations/`, documented its order and safety boundary in `supabase/README.md`, linked the remote `PokeAlliance Codex` project and applied all seven migrations.
- Added `scripts/research/validate-phase2-model.ps1` and generated `data/reports/phase2-model-validation.json`; the audit found zero issues and reran the Phase 1 research validator successfully.
- Initialized the Astro 7 application with React, strict TypeScript, pnpm, Tailwind v4, customized shadcn/ui components and the Vercel server adapter.
- Added localized `/es/` and `/en/` routing, metadata, responsive accessible navigation, product design tokens and the Server Save Temporal display.
- Added the Supabase public environment/client contract while keeping the service-role key server-only; the browser now uses the public client for account sessions and the protected guild RPCs.
- Added Prettier, ESLint, Astro type checking, Vitest, Playwright, data validators, CI workflow and Vercel configuration.
- Added Phase 3 foundation routes for Home, Pokédex, Guías, Mapa and Sistemas; the map route now contains the verified client-coordinate preview while the reviewed Wiki Core surfaces consume real sample records.
- Published the Phase 3 exit audit: [PHASE_3_EXIT_REPORT.md](PHASE_3_EXIT_REPORT.md).
- Added the typed content repository at `src/lib/content/`, reading normalized datasets plus source/evidence registries without embedding research records in UI components.
- Implemented the first Wiki Core slice: localized Home discovery, Pokémon index/detail, guides, systems, rotations/tiers and catalog search. The original public provenance view was later removed by owner decision while its internal contracts were preserved.
- Preserved evidence trails and per-fact status internally for research, reconciliation and correction, without exposing editorial metadata in player-facing cards and articles.
- Added content-repository unit coverage and expanded browser smoke coverage to detail navigation, search and guides; the Playwright server wrapper works in local reuse and CI mode on Windows.
- Published the Phase 4 exit audit: [PHASE_4_EXIT_REPORT.md](PHASE_4_EXIT_REPORT.md).
- Recorded the owner correction and design gate in [DESIGN_DIRECTION.md](DESIGN_DIRECTION.md): Alliance Codex is a modern wiki, not a landing page; tools belong inside a wiki section; no visual system is approved yet.
- Added the owner's anti-AI quality requirement to the design gate: authored wiki voice, concrete source-backed copy, no generic marketing language or decorative generated-UI patterns, and an editorial review checklist for every screen.
- Recorded the three owner-provided modern-wiki screenshots as the visual reference and completed the Phase 3A exit report.
- Reworked the local home discovery, Pokédex and guide presentation in response to the latest owner visual correction: an image-led Pokémon grid, type accents, responsive cards, compact filters/sort/pagination, artwork-forward detail pages, subtle transitions, reduced-motion handling, an actionable home entry surface, and a mission timeline/reward/location layout. The UI contains no public source badges or provenance section; research evidence remains internal.
- Rebuilt the provisional landing-like shell as a wiki: grouped sidebar, global search, compact portal, breadcrumbs, article summary, documentary result lists, evidence trail, responsive navigation and quiet neutral tokens.
- Continued the owner-approved RubinoT wiki reference across the shared shell and localized home portal: the sidebar now uses restrained route iconography, the global/home search is a reusable shadcn `Input` + `Kbd` composition, and the home inventory uses reusable shadcn `Card` panels with dense collection, tool and community indexes instead of tall generic feature cards. Existing routes, reviewed counts, provenance boundaries and bilingual copy remain intact; no generated imagery was needed.
- Added the first wiki-native Tools and Changes routes; Changes is explicitly manual-only until official Discord access changes.
- Added the first Phase 5 tool: a localized coordinate preview with 119 client-observed minimap markers, floor/category filters, selection details and an explicit semantic-data boundary.
- Extended the map preview with client-style legend/layer visibility, zoom, recenter, pointer-drag panning, floor controls and cursor coordinates; the provided client screenshots are documented as interaction references, not map data.
- Added the current OTMM-backed base preview for floors 1/3/4/5/6/7/8/9, with a reproducible extraction script, source hash and coordinate bounds; semantic layers remain unresolved.
- Removed semantic overreach from the map legend: labels outside the known client categories now appear as “Otro marcador del cliente” until a reviewed relation identifies them as Pokémon, NPC, quest or another feature.
- Added the localized map contribution guide at `/es/mapa/aportar/` and `/en/mapa/aportar/`, with required evidence, Temporal-aware capture instants and explicit pending/approved/rejected/stale states. It is documentation-only until moderation and submission infrastructure exist.
- Registered the official teleport guide as a semantic-only map source for region/city/destination aliases; it is explicitly excluded from coordinate placement because it contains no structured x/y/z data.
- Confirmed the community Hoenn Hunts index as a second semantic/visual map source: 137 Pokémon entries with external map-album links are now tracked as reviewable leads, not automatic coordinate records.
- Added the future [Pokédex location capture protocol](POKEDEX_LOCATION_CAPTURE_PROTOCOL.md): a narrow, read-only observation around “Buscar Ubicación”, with credentials/session data excluded and no gameplay automation. It is planned research, not an active dependency.
- Added the public roster importer at `scripts/research/import-public-pokemon-roster.ps1` and its auditable staging snapshot at `data/staging/pka-admin-wiki.pokemon-roster.json`: 910 public variant rows, SHA-256 `69EDE079ED185E3EB1E7C8679DDC1C6D75AABC61CC811D26830F261F716DD8CD`, status `public_staging_snapshot`.
- Implemented the wiki-native Compare Pokémon and Tier Explorer at `/es/herramientas/pokemon/` and `/en/herramientas/pokemon/`. It compares published identity/tier/role/element fields, filters the public roster and keeps missing fields visible as “—”; it does not imply damage formulas, recommendations, movesets or spawn coordinates. See [PHASE_5_POKEMON_EXPLORER_REPORT.md](PHASE_5_POKEMON_EXPLORER_REPORT.md).
- Implemented the localized Guild Ranking tool at `/es/herramientas/guild/` and `/en/herramientas/guild/`. It validates client JSON exports, derives the Monday-to-Sunday week from `exportedAt` using `00:00[America/Sao_Paulo]`, separates daily and donation pace, includes contribution in WhatsApp/PNG exports and preserves the local history path. See [PHASE_5_GUILD_RANKING_REPORT.md](PHASE_5_GUILD_RANKING_REPORT.md).
- Updated the guild ranking calculation with owner-provided level bands (`0–149 Normal = 150`, `150–349 Wildscape = 300`, `350+ Primal = 600`), independent normal/premium goal matrices for total points, dailies and contribution, optional daily/weekly targets, and per-level residual contribution pacing. Direct game corroboration and owner visual acceptance remain pending.
- Added the daily-history engine, authenticated account panel and ordered Supabase foundation for guild administration: the tool accepts multiple cumulative exports with one row per canonical local date, can create/select a guild, saves through the owner/officer RPC and rehydrates saved daily exports/member totals after login. The remote schema, RLS boundary and anonymous RPC denial are verified; invitations, deletion policy and owner acceptance remain later work.
- Simplified the guild-calculation workflow: valid imports now register immediately, same-day imports replace the existing date and recalculate weekly deltas, and every recorded day can restore its ranking and source JSON without a second confirmation step.
- Added a history overview for loaded records, covered weeks and months, with real date ranges, observed-day coverage and incomplete-period labels. Baseline exports are labeled as cumulative rather than being presented as invented daily deltas.
- Added configurable guild difficulty allocation: the tool calculates Normal/Wildscape/Primal counts per snapshot interval, detects tier transitions, exposes current/previous/review estimation policy, and lets the operator correct a member's interval through a validated table editor. The correction updates ranking totals and WhatsApp/Discord breakdowns and is retained in browser storage until durable correction storage is added to the account schema.
- Added deterministic guild member lifecycle analysis over the saved snapshots: per-player level gains, observed joins, departures and returns, plus daily eligibility from the next Server Save and contribution eligibility from the second. Goal bands now use each member's eligible days, the UI exposes a weekly activity panel and per-row access state, Discord daily history includes the aggregate movements, and replacing a same-date snapshot rebuilds the derived lifecycle. Existing Supabase snapshot/member-total persistence already contains the required facts, so no parallel remote table or migration was added.
- Applied the second visual correction of the shared wiki shell from the owner's RubiOT reference: the home route now uses an in-content identity/search block, compact featured links and restrained catalog panels; deep pages keep a centered topbar search, persistent grouped navigation and the same neutral dark panel system. The guild tool remains inside the wiki shell without changing its data or interaction contracts.
- Reconstructed the visual system against measured RubinOT geometry and computed styles. `src/styles/wiki-reference.css` now owns the exact Verdana-based palette, 64/208/1536/208 shell geometry, 40 px shell gaps, flat panel headers and responsive behavior; `AppLayout`, the home portal, wordmark and shared shadcn primitives use that contract without gradients, glows, glass effects or card shadows.
- Removed the double-border failure mode from shared shadcn primitives: cards keep one real 1 px border, while buttons, inputs, badges and linked records use a separate focus outline instead of Tailwind `ring` shadows. The home search, quick links and panels are covered by computed-style assertions for four equal 1 px sides, 12 px radius and no shadow.
- Reorganized Guild as a wiki-native operational workspace while preserving its current calculations, history, exports, difficulty corrections and Supabase persistence. Empty state prioritizes Account/Archive and Import; loaded state prioritizes summary, calculation, export and member table before history/account/settings. Added accessible `Acceso`/`Registro` auth tabs plus member search and factual views for all, on-target, below-target and unscored members. No unsupported kick/capture semantics were introduced.
- Restyled the Guild PNG export with Verdana, the shared neutral tokens, a 1 px outer border and unsaturated rows. Critical Catch informed functional hierarchy only; its illustrated hero, dominant yellow, glows and metric-card aesthetic were not copied.
- Moved the interactive map layer legend out of the plotting surface. It now shares the flat panel language and cannot cover or intercept marker clicks; the map remains internally interactive and document-width containment is preserved on mobile.
- Published the complete current foundation to [GitHub](https://github.com/voidexiled/PokeAlliance-Codex) as the public repository `voidexiled/PokeAlliance-Codex`; the initial commit contains the application, reviewed data, documentation, migrations, public map assets and tests while local secrets, client files and generated directories remain ignored.

## Important findings so far

- The administrator-maintained wiki home reports 530 Pokémon, while its all-Pokémon route reports 910 rows including variants. Counting unit/identity rules remain unresolved.
- The community site reports 425 Pokémon while its repository contains 847 tier/moveset rows; headline counts cannot be compared directly.
- PokeAlliance Shiny tiers such as `Legendary` and `Mythic` are game-specific rarity classifications, not traditional franchise species categories.
- Current Star guidance indicates a five-Star maximum and incompatibility between Stars and Mega Stone.
- The official-candidate wiki is a client-rendered SPA and does not expose a working sitemap at the conventional paths tested.
- User-provided Discord rules permit RMT outside official channels but prohibit price-based RMT advertisements in official game/Discord/forum channels; this does not by itself authorize every Alliance Codex marketplace design. The website terms dropped their explicit RMT permission on 2026-09-17; the Discord rules have not been rechecked since 2026-09-09.
- The local minimap cache supports a future coordinate/floor-aware map, but it does not currently identify cities, hunts, NPCs, quests or teleport points.
- The official public Pokémon roster and the inspected community location datasets do not currently supply coordinate-level spawn markers; this is a documented research boundary, not a claim that the game lacks the feature.
- The official teleport guide can improve map labels and travel semantics, but it does not solve the Pokémon-location payload or justify coordinate inference.
- The community Hoenn Hunts index can supply map images for a meaningful subset of hunts; each image still needs manual coordinate alignment.
- A future client-visible network observation may identify the Pokémon-location relation, but it must remain scrubbed, one-action scoped and pending review; no network interception or replay has been performed.
- The site reads every registry under `content/` at build time. The Pokémon roster has 910 variants (checked 2026-09-23), and the registries the owner writes (OG-12) carry `borrador` placeholders wherever real values are still missing.
- Every route renders in the Corte 0 `PageLayout`; live content coverage remains a sample rather than production-wide coverage, and the owner's page-by-page visual acceptance (S22) is pending.

## Validation status

- Corte 0 (2026-09-19 to 2026-09-23): each milestone was checked when it was integrated, and D-015 to D-026 record the budgets measured at each close. The single complete verification of the cut (`pnpm run ci`, `pnpm test:e2e`, `pnpm test:visual`, the accessibility and performance runs) runs once, with `PUBLIC_SUPABASE_URL` and `PUBLIC_SUPABASE_ANON_KEY` blank, before the preview deploy; the owner then tests manually. The gate is `pnpm run ci`: bare `pnpm ci` is pnpm's own clean-install command.
- M15 close (2026-09-23), with the Supabase public settings blank: `pnpm install --frozen-lockfile` is up to date; `pnpm run ci` passed (content check, `design:check` over 290 files with no exclusion, format, ESLint, `astro check` 0/0/0 over 319 files, `i18n:check` with 572 keys, Vitest 57 files / 1.202 tests, build, `check-toc`, `check-dist` over 1.896 pages and 2 on demand with the shared sheet at 98.922 B, `perf:budget` over 1.896 routes and 2 on demand, `seo:check` over 1.896 routes); `pnpm exec playwright test tests/e2e/smoke.spec.ts --project=desktop` passed 107/107. The search `AppLayout|styles/legacy|AllianceWordmark|localeCopy|localeLabels|--wiki-|--trade-|--dex-|view-transition` over `src/` returns nothing, and so do the old class and variable searches of D-026. The rest of the single complete verification (full e2e, visual, accessibility, performance) is still to run.
- D-012 provenance removal (2026-09-18): `pnpm check` (0 errors, 1 existing `document.execCommand` hint), `pnpm lint`, `pnpm test` (8 files, 71 tests), `pnpm format:check` and `pnpm build` passed. `tsconfig.json` now excludes `research-inbox/`, whose untracked community code dumps had 148 type errors. Playwright passed 10/10 against a fresh dev server; the long-running dev server on port 4321 answered `504 Outdated Optimize Dep` for React islands and needs a restart. The two local migrations were not run.
- Local visual-redesign verification (2026-09-16): `pnpm run ci` passed format, lint, Astro check, research/model validators, 29 Vitest tests and the Astro/Vercel build. `pnpm exec playwright test --workers=1` passed 8/8, including Pokédex image fallback, search, variant/type filtering, empty state, pagination, and desktop/mobile route containment across both locales. Home and Guías were visually inspected at desktop and 390 px mobile; the Pokédex grid was inspected at desktop. The existing `document.execCommand` deprecation hint remains. Owner visual acceptance and deployment are still pending.
- `git diff --check`: passed.
- `pnpm run ci`: passed through format check, ESLint, Astro check, research validation, Phase 2 validation, Vitest and Astro/Vercel build after the guild-ranking slice.
- Application checks: `pnpm format:check`, `pnpm lint`, `pnpm check` (58 files, 0 errors), `pnpm test` (6 files, 18 tests), `pnpm build` and `pnpm test:e2e` (3/3) passed after the guild daily-history slice. Astro reports one deprecation hint for the browser-only `document.execCommand` clipboard fallback; it is not a build or type error.
- After the level-based guild goal update: `pnpm check` passed with 0 errors and one existing clipboard deprecation hint; `pnpm test` passed with 6 files and 20 tests.
- After the level-based guild goal update: `pnpm run ci` passed through build, and `pnpm test:e2e` passed 3/3 with parallel workers; the guild route was also verified against the real owner-provided export with no browser console errors.
- Guild sharing update: WhatsApp export is now localized per route (Spanish on `/es/`) and formatted as readable member blocks with level, dailies, contribution, total points, rank and last access. Discord export now uses Markdown, includes loaded daily/monthly aggregates without inventing missing history, and splits long rankings into ordered blocks under Discord's message limit.
- Guild sharing validation: `pnpm test` passed with 6 files and 21 tests, `pnpm check` passed with 0 errors and the existing `document.execCommand` deprecation hint, `pnpm lint` passed, `pnpm build` passed and `pnpm exec playwright test --workers=1` passed 3/3. The live browser was also checked with the real owner-provided JSON: both export buttons render and the copy actions now fall back when Clipboard API permission is unavailable.
- Guild history and difficulty workflow validation: `pnpm run ci` passed through format, lint, Astro check, research/Phase 2 validation, Vitest (6 files, 23 tests) and Astro/Vercel build; `pnpm exec playwright test tests/e2e/smoke.spec.ts --workers=1` passed 3/3, including opening and saving a manual difficulty breakdown. Astro reports the existing `document.execCommand` clipboard deprecation hint only.
- Guild lifecycle readiness validation: `pnpm run ci` passed through format, lint, Astro check, both data validators, Vitest (6 files, 27 tests) and the Astro/Vercel build; `pnpm exec playwright test tests/e2e/smoke.spec.ts --workers=1` passed 4/4. The dedicated Monday-to-Tuesday scenario verifies observed joins, per-member level gains, daily access on `d+1`, contribution access on `d+2`, eligibility-adjusted goal bands and same-date recalculation. The loaded state was visually inspected at desktop width; the existing clipboard deprecation hint remains the only diagnostic.
- Browser checks: Playwright smoke suite passed 3/3, including localized routing, Pokédex search/detail, guides, sources, guild JSON file loading, contribution table coverage, console/page-error checks and absence of the Astro error overlay. `agent-browser` verified the live guild route, screenshot, interactive snapshot, no console errors, no framework overlay and successful local JSON rendering.
- Visual shell correction validation: `agent-browser` inspected the Spanish home and Pokémon detail at 1440×900, the home at 375px, and the guild route after the shared CSS/layout update. The intended wiki hierarchy is visible in all three states; no new console or page errors were observed. Owner visual acceptance remains pending.
- RubinoT-reference continuation validation: the Spanish home was inspected at 1440×900 and 390×844, and the shared Pokédex shell at 1440×900. `pnpm run ci` passed (format, lint, Astro check, both data validators, 6 Vitest files / 27 tests and production build); `pnpm exec playwright test --workers=1` passed 4/4. The pre-existing `document.execCommand` deprecation hint remains the only Astro diagnostic. Owner visual acceptance remains pending.
- Full RubinOT-system reconstruction validation (2026-09-11): `pnpm run ci` passed format, ESLint, Astro check, both data validators, 6 Vitest files / 27 tests and the Astro/Vercel production build. `pnpm exec playwright test --workers=1` passed 6/6. New browser coverage measures Verdana, the 2518 px shell position/1536 px content width, equal 1 px borders, 12 px radii and no shadows; it also checks all 28 localized route surfaces in `es` and `en` at 2518×1376 and 390×844 for document overflow, broken images and framework overlays, loading the Guild fixture in both languages and widths. Guild member search/filter behavior, PNG generation and unobstructed map-marker selection are exercised. The only Astro diagnostic is the existing browser clipboard fallback deprecation hint; owner visual acceptance and authenticated owner-account acceptance remain pending.
- Vercel production deployment (2026-09-11): the locally verified RubinOT-system reconstruction was built with Vercel CLI 50.9.5 and Astro's Vercel adapter, then deployed from the uncommitted `master` checkout at base commit `5c76ddf`. Deployment `dpl_Dz8iXqRZVjP3yPDVkb6MwJLGhupL` is `Ready` and aliased to [pokealliance-codex.vercel.app](https://pokealliance-codex.vercel.app). The root redirect, Spanish home, Spanish and English Guild routes, and Spanish Map route returned HTTP 200 after publication; the runtime-log observation produced no errors while those routes were requested.
- Guild-goal classification hotfix (2026-09-12, production): goal drafts now drive ranking and history calculations immediately instead of leaving the visible classification on the previously applied settings until the confirmation button is pressed. Ranking rows expose the exact unmet standard metrics with observed/expected values, so a member who satisfies dailies but remains below the still-active total-points target is no longer ambiguous. A focused unit test covers the combined-metric explanation, and Playwright verifies that clearing the Thursday total-points target changes Alpha from `Por debajo` to `En meta` without pressing recalculate while Beta reports `Falta: Dailies 2/4`. `pnpm run ci` passed with 6 test files / 28 tests and the Astro/Vercel build; `pnpm exec playwright test tests/e2e/smoke.spec.ts --workers=1` passed 6/6 after replacing a stale Vite server that had returned `504 Outdated Optimize Dep`. Vercel deployment `dpl_w3Ychf9F6uUtbVQoFTK41WP7t5EC` is `Ready` and aliased to [pokealliance-codex.vercel.app](https://pokealliance-codex.vercel.app). A production browser check reproduced `En meta 0` before the goal edit and `En meta 1` immediately afterward, confirmed Alpha's `En meta`, Beta's `Falta: Dailies 2/4`, and found no console or page errors.
- Guild workspace simplification (2026-09-12, production): importing a valid JSON now adds or replaces its Server Save date immediately; browser storage retains up to 120 daily exports, goals, difficulty values/policy and manual corrections. The history is a date selector that restores both the selected ranking and its original JSON. Baseline pacing begins on the first observed date, so a week first tracked on Wednesday expects one eligible day on Wednesday and two on Thursday instead of charging missing Monday/Tuesday data. Account and goal configuration use collapsed progressive disclosure, the redundant calculation-control panel was removed, the historical overview was condensed, row actions are icon-sized and the mobile ranking presents only essential columns. `pnpm check` passed with 0 errors and the existing clipboard hint; Vitest passed 6 files / 29 tests; Playwright passed 6/6 including direct two-day registration and 390 px containment. A live local browser check confirmed 2 records after importing September 10 and 11, date-to-JSON navigation, and persistence of both history and an edited goal after reload.
- Guild operational-layout redesign (2026-09-12, production): the loaded tool is now a compact two-zone workspace at wide desktop widths. The ranking table is the dominant surface with its own vertical scroll and sticky header; a narrow rail contains the daily snapshot selector plus collapsed Activity, Account/backup, Import/JSON and Goals/calculation utilities. Export actions form a single compact command bar, summary metrics are shorter, snapshot rows contain only date/dailies/contribution/status, and feedback remains globally visible when a utility closes. At narrower widths the same content becomes one contained column. The 1920×1080 loaded view was visually checked with the fixture; `pnpm run ci` passed with 6 test files / 29 tests and the Astro/Vercel build, and Playwright passed 6/6 including the redesigned desktop/mobile containment, direct two-day registration and history workflow.
- Vercel production deployment (2026-09-13): the validated uncommitted `master` checkout at base commit `5c76ddf` was rebuilt locally against the production project settings and deployed as a 1.2 MB prebuilt artifact after the normal source upload exceeded Vercel's free-plan file-request quota. Deployment `dpl_5SFT6ZeaAsQpWt4VNJyGc8Zxfj3k` is `READY` and aliased to [pokealliance-codex.vercel.app](https://pokealliance-codex.vercel.app). The Spanish home, Spanish and English Guild routes and Spanish Map route returned HTTP 200; the Spanish Guild response contains the new `Cuenta y respaldo`, `Importar JSON` and `Metas y cálculo` interface copy. No runtime error events were emitted during the post-deployment route probes.
- Guild single-workspace reconstruction (2026-09-14, local): removed the permanent utility rail and repeated summary/export panels. Guild now has one compact context header, mutually exclusive Ranking/Activity/History views, a ten-row ranking, one import dialog and contextual sheets for goals, account persistence and member details/corrections. Historical mode is explicit, JSON remains available per snapshot, and the mobile layout has no document-level horizontal overflow at 375 px. Goal configuration now uses explicit save/cancel semantics, validates values before persistence and restores the saved matrix after cancellation; the workspace also distinguishes hydration, offline browser storage, missing snapshot days and PNG-generation failure. `pnpm run ci` passed with 6 Vitest files / 29 tests and the Astro/Vercel build; Playwright passed 7/7, including 10-row pagination, one-sheet behavior, invalid-goal cancellation, saved-goal reload, two-day lifecycle/history and containment across all localized routes. Local browser inspection covered 1920×1080, the goals and member sheets, and 375 px mobile with no console/page errors. This local change has not been deployed; owner visual and authenticated owner-account acceptance remain pending.
- GitHub publication validation: the explicit non-ignored file allowlist staged 175 foundation files, followed by 8 repository tooling/policy files; no `.env`, client-inbox, dependency, build-output or CodeGraph files were included. The published `master` history contains 183 tracked project files at [voidexiled/PokeAlliance-Codex](https://github.com/voidexiled/PokeAlliance-Codex).
- GitHub Actions validation: workflow run [34589946041](https://github.com/voidexiled/PokeAlliance-Codex/actions/runs/34589946041) passed both quality and browser jobs; the browser job reports 4 passing Playwright tests. GitHub still shows the upstream Node 20 action deprecation annotation for `actions/checkout@v4`, `actions/setup-node@v4` and `pnpm/action-setup@v4`.
- PostgreSQL/Supabase verification: the linked remote project has all seven migrations applied; RLS/advisor checks and anonymous RPC denial passed. Authenticated account creation/import acceptance still requires a real owner-controlled account and fixture.
- Clean-machine install and owner visual/runtime acceptance: not performed.

## Carry-forward items for Phase 5 and later

- The map is a placeholder page since M11; `MapExplorer.tsx`, `content/map/*` and `src/lib/map/*` are kept unused for its own cut (R9, spec §15). When it opens, continue by comparing the derived OTMM surface with the client and correlating semantic coordinate boundaries; the coordinate, base-preview and client-style interaction slices were verified before Corte 0.
- Keep the Pokémon roster refreshable with `pnpm content:roster`; reconcile species identity, aliases and richer variant semantics before treating the 910 rows as complete Pokémon profiles.
- Expand movesets, evolutions, locations and aliases in `content/` before enabling tools that would imply complete field coverage; `UK-010` remains open.
- Complete owner-controlled Auth acceptance with a disposable account and anonymized fixture; verify sign-in, guild creation, same-day replacement and rehydrated history against the remote project once its pending migrations are applied (OG-1).
- Determine reuse/licensing boundaries for source text, datasets and media before production asset copying.
- Capture Discord changelog messages manually; bot authorization is not an active dependency.
- The second read-only local sweep did not locate a readable data path behind the in-game Pokédex “Buscar localização” action; use a permitted client-visible capture/public resource or reviewed community capture when one becomes available.
- The dedicated local location helper modules and image files remain protected/nonstandard; no additional placement data was taken from them.
- Correlate semantic map labels with the OTMM/config marker coordinates without treating colored tiles as place names.
- Validate a before/after guild export pair around `00:00[America/Sao_Paulo]`; only one structural owner export has been profiled so far. The tool now supports the multi-snapshot calculation path, but real longitudinal guild history still requires owner-provided exports from additional Server Save dates.
- Begin or continue the live guild history with a consistent one-export-per-Server-Save-date cadence. The first loaded roster is an explicit baseline and pacing begins on that first observed date even if it is midweek; exact join instants cannot be recovered from the JSON, so first appearances remain conservative observed intervals under `U-010`.
- Guild invitations, roles, «Quitar» and «Eliminar guild» exist since M13, against the local stack only; a retention policy and an explicit member-consent policy remain before treating the guild administration as production-ready.
- Expand the map reader only after semantic coordinate labels are available; `/{l}/mapa/` is a placeholder and `/{l}/mapa/aportar` redirects to it until a real submission channel exists.

## Exact next action

The owner reviews the local preview page by page against its board (S22) and accepts or corrects it; until then the cut stays «implemented and technically verified; visual acceptance pending». M14 (Comercio phase B) continues in parallel, local only. The owner fills the D-011 and Corte 0 registries (OG-12) with real values following [REGISTROS.md](REGISTROS.md) and removes `borrador` as each record becomes real; Stage B of [GAME_KNOWLEDGE_EXPANSION_PLAN.md](GAME_KNOWLEDGE_EXPANSION_PLAN.md) continues after that. The migrations that are applied only to the local stack must be applied remotely, with the owner's authorization (OG-1), before any remote database dependency is enabled; the guild client works before and after `20260918220000_remove_provenance.sql` (it retries with a fixed `p_source_locator` while the old function exists; remove the retry once the migration is applied). Guild owner acceptance, authenticated persistence acceptance and the regular Server Save export cadence remain open parallel acceptance items.

## Worktree note

The project foundation is published on `origin/master`. The RubinOT-system reconstruction, Guild behavior fixes, operational-layout redesign, knowledge expansion, visual redesign and the Corte 0 redesign described above remain a local, uncommitted change set on `master`; they have not been pushed to GitHub. The earlier application state was deployed directly to Vercel production on 2026-09-13 as `dpl_5SFT6ZeaAsQpWt4VNJyGc8Zxfj3k`, so that deployment contains none of the later work: the knowledge expansion, the visual slice and Corte 0 are all local. Local `.env`/`.env.local`, `.vercel/`, `.codegraph/`, dependency/build output and owner-provided client files remain ignored and stay on this machine. Future commits should continue using an explicit staged-file allowlist and preserve those local-only paths.
