// «Drop de» of an item panel (§7.5.3) names up to this many Pokémon and counts them past it:
// an Evolution Stone drops from a hundred Pokémon, and a hundred names make a panel no one
// reads and a page over its §13.6 budget. Its own module so the list islands read it without
// the tooltip builders.
export const DROPPER_NAMES_MAX = 6;
